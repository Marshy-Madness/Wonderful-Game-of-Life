# Copy to `.env.local` for local development (Create React App reads env from `client/`).
# Point the UI at your API server (default Express port is 2988).
#
# REACT_APP_API_URL=http://127.0.0.1:2988
